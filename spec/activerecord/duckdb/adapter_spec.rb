# frozen_string_literal: true

RSpec.describe ActiveRecord::Duckdb::Adapter do
  it "has a version number" do
    expect(ActiveRecord::Duckdb::Adapter::VERSION).not_to be nil
  end

  it "does something useful" do
    expect(false).to eq(true)
  end
end
